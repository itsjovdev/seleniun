# No environment variables are required to run this app locally.
#
# In development, Vite proxies /api requests to the backend configured in
# vite.config.ts (defaults to https://api.seleniun.com). To point at a local
# backend instead, edit the proxy target in vite.config.ts, or set:
# VITE_API_PROXY_TARGET=http://localhost:3000
